package cn.hoyobot.sdk.command.data

enum class CommandParamType {
    INT, FLOAT, VALUE, WILDCARD_INT, OPERATOR, TARGET, WILDCARD_TARGET, FILE_PATH, INT_RANGE, STRING, POSITION, BLOCK_POSITION, MESSAGE, TEXT, JSON, COMMAND
}